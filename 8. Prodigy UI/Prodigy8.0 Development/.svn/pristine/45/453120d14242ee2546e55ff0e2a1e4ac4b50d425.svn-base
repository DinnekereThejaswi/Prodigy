import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { IpAddressMngmtComponent } from './ip-address-mngmt.component';

describe('IpAddressMngmtComponent', () => {
  let component: IpAddressMngmtComponent;
  let fixture: ComponentFixture<IpAddressMngmtComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ IpAddressMngmtComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(IpAddressMngmtComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
