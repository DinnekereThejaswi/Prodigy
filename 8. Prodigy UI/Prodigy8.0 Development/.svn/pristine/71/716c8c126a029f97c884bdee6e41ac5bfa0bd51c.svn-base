import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AccCodeSettingComponent } from './acc-code-setting.component';

describe('AccCodeSettingComponent', () => {
  let component: AccCodeSettingComponent;
  let fixture: ComponentFixture<AccCodeSettingComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AccCodeSettingComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AccCodeSettingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
