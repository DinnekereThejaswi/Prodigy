import { ActiveDeactivePipe } from './active-deactive.pipe';

describe('ActiveDeactivePipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveDeactivePipe();
    expect(pipe).toBeTruthy();
  });
});
