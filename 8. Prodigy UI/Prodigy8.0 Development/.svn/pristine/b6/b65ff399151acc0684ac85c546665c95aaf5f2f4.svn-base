import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CancelIssueComponent } from './cancel-issue.component';

describe('CancelIssueComponent', () => {
  let component: CancelIssueComponent;
  let fixture: ComponentFixture<CancelIssueComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CancelIssueComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CancelIssueComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
