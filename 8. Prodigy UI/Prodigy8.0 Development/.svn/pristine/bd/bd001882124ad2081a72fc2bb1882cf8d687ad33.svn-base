import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FixedOrderComponent } from './fixed-order.component';

describe('FixedOrderComponent', () => {
  let component: FixedOrderComponent;
  let fixture: ComponentFixture<FixedOrderComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FixedOrderComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FixedOrderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
