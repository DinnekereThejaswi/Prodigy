<div class="row">
  <div class="col-md-12">
    <div class="card">
      <div class="card-body">
        <form [formGroup]="MainLocationForm" class="form-horizontal">
          <div class="row">
            <div class="col-md-3">
              <div class="form-group">
                <label for="counterCode"> Code </label>
                <input oninput="this.value = this.value.toUpperCase()" class="form-control"
                  formControlName="MainCounterCode" (input)="Codes.value = Codes.value.toUpperCase()"
                  [(ngModel)]="MainLocationListData.MainCounterCode" [readonly]="isReadOnly" placeholder="Enter code"
                  type="text" #Codes>
              </div>
            </div>
            <div class="col-md-3">
              <div class="form-group">
                <label for="counter Name"> Name </label>
                <input type="text" class="form-control" [(ngModel)]="MainLocationListData.MainCounterName"
                  formControlName="MainCounterName" AlphabetFieldValidator maxlength="30" placeholder="Enter Name">
              </div>
            </div>
            <div class="col-md-3">
              <div class="form-group">
                <ng-container *ngIf="EnableAdd">
                  <button class="btn btn-success waves-effect waves-light btn-rounded btn-secondary MT-2"
                    style="margin-top: 1.2rem !important;" (click)="add(MainLocationForm)">
                    <i class="far fa-plus-circle"></i>&nbsp;Add</button>
                </ng-container>
                <ng-container *ngIf="EnableSave">
                  <button class="btn btn-success waves-effect waves-light btn-rounded btn-secondary MT-2"
                    style="margin-top: 1.2rem !important;" (click)="save(MainLocationForm)">
                    <i class="fal fa-save"></i>&nbsp;Save</button>
                </ng-container>
                <button class="btn  btn-warning waves-effect waves-light btn-rounded btn-secondary MT-2"
                  style="margin-top: 1.2rem !important;" (click)="clear()">
                  <i class="fal fa-trash"></i>&nbsp;Clear</button>
              </div>
            </div>
            <div class="col-md-2"></div>
            <div class="col-md-1">
              <button type="button" class="btn btn-success waves-effect waves-light btn-rounded btn-secondary MT-2"
                style="margin-top: 1.2rem !important;" (click)=Print() id="exampleModalLabel"><i
                  class="fal fa-print"></i>
                Print</button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
  <div class="col-md-12">
    <div class="card">
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-bordered  table-hover ">
            <thead class="topbar">
              <tr>
                <th> Code</th>
                <th> Name</th>
                <th>Status</th>
                <th>Edit</th>
                <th>Open</th>
                <th>Close</th>
              </tr>
            </thead>
            <tbody>
              <tr *ngFor=" let  _counters of MainLocation; let i = index">
                <td>{{_counters.MainCounterCode}}</td>
                <td>{{_counters.MainCounterName}}</td>
                <td [style.color]="getStatusColor(_counters.ObjectStatus)">
                  <b>{{_counters.ObjectStatus | statusName : _counters.ObjectStatus }}</b>
                </td>
                <td align="center"><i class="fas fa-edit" title="edit" (click)="editField(_counters)"></i>
                </td>
                <td align="center">
                  <i class="fas fa-lock-open" title="open" (click)="openField(_counters)"></i>
                </td>
                <td align="center">
                  <i class="fas fa-lock" title="close" (click)="closeField(_counters)"></i>
                </td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <ng-container *ngIf="EnableJson">
    <pre>{{MainLocationListData | json}}</pre>
  </ng-container>

  <div class="modal fade" id="MainLocationDetailsTab" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
    aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document" style="max-width: 900px">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button"
            class="btn btn-outline-success waves-effect waves-light btn-rounded btn-outline-secondary"
            id="exampleModalLabel" (click)=print()><i class="fal fa-print"></i>
            Print</button>
          <h3 class="modal-title">{{bcode}}-Location List</h3>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <div class="row">
            <div class="col-md-12">
              <div class="table-responsive" id="TableData">
                <table class="table table-bordered table-hover">
                  <thead class="topbar">
                    <tr>
                      <th>Main Counter Code</th>
                      <th>Main Counter Name</th>
                      <th>Status</th>
                    </tr>
                  </thead>
                  <tbody *ngFor="let _counters of printData;let i=index">
                    <tr>
                      <td>{{_counters.MainCounterCode}}</td>
                      <td>{{_counters.MainCounterName}}</td>
                      <td>{{_counters.ObjectStatus}}</td>
                    </tr>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>